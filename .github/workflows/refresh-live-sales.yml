name: Refresh Live Sales

on:
  # Primary trigger: cron every 15 min during 7am-9pm ET window.
  # Covers both DST (UTC-4) and standard (UTC-5) time:
  #   DST:      UTC 11 = 7am ET ... UTC 1 = 9pm ET  (UTC 2 = 10pm, harmless overrun)
  #   Standard: UTC 11 = 6am ET ... UTC 2 = 9pm ET  (UTC 11 = 6am, harmless early)
  # workflow_dispatch is kept for ad-hoc manual runs.
  # NOTE: GitHub Actions cron can drift 0-15 min under load, so actual cadence
  # is "approximately every 15 min" which is the intended freshness target.
  schedule:
    - cron: '*/15 11-23 * * *'
    - cron: '*/15 0-2 * * *'
  workflow_dispatch: {}

concurrency:
  group: refresh-live-sales
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Ping /api/log-live-sales (yoda + snowflake)
        run: |
          set -u
          BASE="https://aubuchon-it-command-center.vercel.app/api/log-live-sales"
          # Pipelines: label + URL. Both run each pass; same prediction engine,
          # different live-sales source, different per-source log dir.
          SOURCES="yoda:${BASE} snowflake:${BASE}?source=snowflake"
          for PAIR in $SOURCES; do
            LABEL="${PAIR%%:*}"
            URL="${PAIR#*:}"
            echo "=== [$LABEL] $(date -u +%FT%TZ) ==="
            CODE=$(curl -s -o /tmp/resp.json -w "%{http_code}" --max-time 180 "$URL" || echo "curl_err")
            echo "HTTP $CODE"
            if [ "$CODE" = "200" ]; then
              python3 -c "
          import json
          try:
              d = json.load(open('/tmp/resp.json'))
              p = ((d.get('prediction') or {}).get('prediction')) or {}
              cur = (d.get('prediction') or {}).get('current') or {}
              print('  status:', d.get('status'))
              print('  source:', d.get('source'))
              print('  tsET:', cur.get('tsET'))
              print('  sales:', cur.get('sales'))
              print('  plan:', cur.get('plan'))
              print('  method:', p.get('method'))
              print('  projectedEOD:', p.get('projectedEOD'))
              print('  confidence:', p.get('confidence'))
          except Exception as e:
              print('  parse err:', e)
          "
            else
              echo "  body: $(head -c 300 /tmp/resp.json 2>/dev/null || echo '-')"
            fi
          done
